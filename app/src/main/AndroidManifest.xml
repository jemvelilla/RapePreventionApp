<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.build1.rapepreventionapp">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/hp"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Welcome"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".RegisterStep1"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".RegisterStep2"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".RegisterStep3"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".RegisterStep4"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".AccountCreated"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".Login"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".Slides"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".ForgotPassStep1"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".ForgotPassStep2"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".ForgotPassStep3"
            android:screenOrientation="portrait"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBC3ugj9ifSjas3AuqWFOXCyoSSE6sxDXI" />

        <activity
            android:name=".Home"
            android:label="@string/title_activity_home"></activity>
    </application>

</manifest>